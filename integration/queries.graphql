query GetAuthorSummaries {
  authorSummaries {
    author {
      name
    }
  }
}
mutation SaveAuthor($input: AuthorInput!) {
  saveAuthor(input: $input) {
    author {
      name
    }
  }
}

query CurrentAuthor {
  currentAuthor {
    name
  }
}
